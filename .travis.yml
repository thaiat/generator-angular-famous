language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-famous' ]; then cd .. && eval "mv $currentfolder generator-angular-famous" && cd generator-angular-famous; fi

